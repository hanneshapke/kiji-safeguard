# OpenAI credentials used by CrewAI (loaded automatically by main.py via python-dotenv).
# Replace the placeholder with your real key, rename file to ".env" before running.
OPENAI_API_KEY=sk-replace-me

# Optional: override the model the crew uses (default: gpt-4o-mini).
OPENAI_MODEL_NAME=gpt-4o-mini
